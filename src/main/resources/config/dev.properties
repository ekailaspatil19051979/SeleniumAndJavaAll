# Development Environment Configuration
environment=dev
app.base.url=https://the-internet.herokuapp.com

# Database Configuration (if needed)
db.url=jdbc:h2:mem:testdb
db.username=sa
db.password=

# API Configuration (if needed)
api.base.url=https://the-internet.herokuapp.com/api
api.timeout=30

# Test Configuration
test.retry.count=1
test.parallel.enabled=true
test.thread.count=3